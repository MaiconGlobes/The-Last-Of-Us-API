import { Grid } from '@mui/material';
import ToolBar from '../../frames/appbar';

const HeaderSession = () => {
  return (
    <Grid item columns={1}>
      <ToolBar />
    </Grid>
  );
};

export default HeaderSession;
