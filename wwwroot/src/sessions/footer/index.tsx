import { Grid } from '@mui/material';

const FooterSession = () => {
  return (
    <Grid
      item
      columns={1}
      id='footer-session'
      sx={{
        background: '#000007',
        position: 'relative',
        minHeight: '25px',
      }}>
      {/* rodape */}
    </Grid>
  );
};

export default FooterSession;
